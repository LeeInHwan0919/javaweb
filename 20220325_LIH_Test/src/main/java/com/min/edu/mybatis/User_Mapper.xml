<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.min.edu.model.UserDaoImpl">
    <insert id="userInsert" parameterType="uDto">
    INSERT INTO GDUESR (ID, PASSWORD, NAME, REGDATE)
        VALUES(#{id},#{password},#{name},#{regdate})
    </insert>


    <select id="selectId" parameterType="uDto" resultType="uDto">
      SELECT ID
          FROM GDUESR
          WHERE ID=#{id}
    </select>
    
    
    
</mapper>