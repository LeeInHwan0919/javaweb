<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.min.edu.dao.PlayerImpl">
	<select id="showView" resultType="PlayerVo">
		<![CDATA[SELECT * FROM(
			SELECT ROW_NUMBER() OVER(ORDER BY ROWNUM ASC) AS RN, PLAYER_NAME, TEAM_ID, "POSITION" 
			FROM PLAYER p) WHERE RN >= #{startnum} AND ROWNUM <= 10]]>
	</select>
	
	<select id="count" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM PLAYER
	</select>
</mapper>
